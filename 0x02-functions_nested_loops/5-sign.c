#include <stdio.h>
#include "main.h"

/**
 * int print_sign(int n) - checks if number > 0
 *
 */

int print_sign(int n)
{
	if (n > 0)
	{
		return (1);
		printf("+");
	}
	else if (n = 0)
	{
		return (0);
		printf("0");
	}
	else
	{
		return (-);
		printf(-1);
	}
}
